// this includes the basic input/output library to provide printf function
#include <stdio.h>                   

// Entry point of the program, defines a main function to return an integer and takes no arguments
int main(void) {

    // Print a message to the console, \n adds a new lines to console
    printf("Hello, World!\n");
    
    // Return 0 to indicate successful execution, non zero will indicate an error
    return 0;
}
